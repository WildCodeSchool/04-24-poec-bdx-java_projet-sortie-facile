import { AccountNavActiveLinkPipe } from './account-nav-active-link.pipe';

describe('AccountNavActiveLinkPipe', () => {
	it('create an instance', () => {
		const pipe = new AccountNavActiveLinkPipe();
		expect(pipe).toBeTruthy();
	});
});
